# Gemeinsamer Caddy-Einstiegspunkt. Anwendungen liefern versionierte Snippets
# unter deploy/sites; weitere VPS-Zellen koennen eigene *.caddy-Dateien
# ergaenzen, ohne diese Datei oder CVMS-Bloecke manuell zusammenzukopieren.
{
	email {$ACME_EMAIL}
}

import /etc/caddy/sites/*.caddy
